/* import { configure, observable } from "mobx";
import { model } from "./testmodel.js";
import { connectToPersistence, saveModelToFirestoreACB } from "./firestoreModel.js";

configure({ enforceActions: "never" });

export const reactiveModel = observable(model);

// Make reactive model available in console
window.myModel = reactiveModel; // Fixed global */




